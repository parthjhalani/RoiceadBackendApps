VIEW MY_ROICEAD_TOPACTIVECARDS ("COUNTOFACTIVECARDS" , "TYPENAME")
AS
 (SELECT TOP 5
	count(TYPENAME) as COUNTOFACTIVECARDS,
	"TYPENAME"
FROM "MY_ROICEAD_CARDS"
where status = 'Active'
group by "TYPENAME" order by COUNTOFACTIVECARDS desc )