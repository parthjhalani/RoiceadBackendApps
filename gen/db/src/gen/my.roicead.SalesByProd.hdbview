VIEW my_roicead_SalesByProd AS SELECT
  C_CARDS_0."PRODUCT",
  C_CARDS_0.VALUECURRENCY,
  sum(C_CARDS_0.TOTALGROSSVALUE) AS TOTALGROSSVALUE,
  sum(C_CARDS_0.GROSSVALUE) AS GROSSVALUE
FROM my_roicead_C_CARDS AS C_CARDS_0
GROUP BY C_CARDS_0."PRODUCT", C_CARDS_0.VALUECURRENCY